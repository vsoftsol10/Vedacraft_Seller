generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL")
}

model Product {
  id              String   @id @default(uuid()) @db.Uuid
  productId       String   @unique @map("product_id")
  productName     String   @map("product_name")
  category        String
  subCategory     String?  @map("sub_category")
  material        String
  weight          String
  description     String?
  benefits        String?
  highlights      String?
  length          Decimal? @db.Decimal(10, 2)
  width           Decimal? @db.Decimal(10, 2)
  height          Decimal? @db.Decimal(10, 2)
  mrp             Decimal  @db.Decimal(12, 2)
  discountPrice   Decimal? @map("discount_price") @db.Decimal(12, 2)
  sellingPrice    Decimal  @map("selling_price") @db.Decimal(12, 2)
  coverImage      String?  @map("cover_image")
  additionalImages String[] @default([]) @map("additional_images")
  sku             String?  @unique
  stockQuantity   Int      @default(0) @map("stock_quantity")
  lowStockAlert   Int      @default(5) @map("low_stock_alert")
  stockStatus     String   @default("Out of Stock") @map("stock_status")
  howToUse        String?  @map("how_to_use")
  careInstruction String?  @map("care_instruction")
  createdAt       DateTime @default(now()) @map("created_at")
  updatedAt       DateTime @updatedAt @map("updated_at")

  @@map("seller_products")
}

<<<<<<< HEAD
model SellerApplication {
  id              String   @id @default(uuid()) @db.Uuid
  userId          String?  @map("user_id") @db.Uuid
  fullName        String?  @map("full_name")
  businessEmail   String?  @map("business_email")
  mobileNumber    String?  @map("mobile_number")
  alternateNumber String?  @map("alternate_number")
  profileImage    String?  @map("profile_image")
  updatedAt       DateTime @map("updated_at")
  sellingLocations SellerSellingLocation[]

  @@map("seller_applications")
}

model SellerSellingLocation {
  id        String            @id @default(uuid()) @db.Uuid
  sellerId  String            @map("seller_id") @db.Uuid
  state     String
  city      String
  createdAt DateTime          @default(now()) @map("created_at")
  seller    SellerApplication @relation(fields: [sellerId], references: [id], onDelete: Cascade)

  @@index([sellerId])
  @@map("seller_selling_locations")
}
=======
>>>>>>> 78e1cc33d0dad5fb46c601957e814d18b2277c7a
