export default function LowStockAlert({ title = "Low Stock Alert", items }) { return <section className="rounded-lg border border-gray-200 bg-white p-5 shadow-sm"><h3 className="mb-4 font-semibold text-gray-900">{title}</h3><ul className="space-y-3">{items.length === 0 && <li className="text-sm text-gray-500">All products are well stocked.</li>}{items.map((item) => <li key={item.name} className="flex items-center justify-between text-sm"><span className="text-gray-700">{item.name}</span><span className="text-gray-900">Only {item.stock} left</span></li>)}</ul></section>; }
