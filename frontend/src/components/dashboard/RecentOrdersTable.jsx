const statusStyles = { Placed: "bg-blue-50 text-blue-600", Processing: "bg-amber-50 text-amber-600", Packed: "bg-amber-50 text-amber-600", Shipped: "bg-violet-50 text-violet-600", Delivered: "bg-emerald-50 text-emerald-600" };
const money = (value) => new Intl.NumberFormat("en-IN", { style: "currency", currency: "INR", maximumFractionDigits: 0 }).format(Number(value) || 0);
export default function RecentOrdersTable({ orders }) { return <div className="overflow-x-auto rounded-lg border border-gray-200 bg-white p-5 shadow-sm"><h3 className="mb-4 font-semibold text-gray-900">Recent Orders</h3><table className="w-full text-sm"><thead><tr className="border-b border-gray-200 text-left text-gray-900"><th className="pb-2 font-medium">Order ID</th><th className="pb-2 font-medium">Customer</th><th className="pb-2 font-medium">Amount</th><th className="pb-2 font-medium">Status</th><th className="pb-2 font-medium">Date</th></tr></thead><tbody>{orders.length === 0 && <tr><td colSpan={5} className="py-5 text-center text-gray-500">No orders yet</td></tr>}{orders.map((order) => <tr key={order.id} className="border-b border-gray-50 last:border-0"><td className="py-3 text-gray-700">{order.id}</td><td className="py-3 text-gray-700">{order.customer}</td><td className="py-3 text-gray-700">{money(order.amount)}</td><td className="py-3"><span className={`rounded-full px-2 py-1 text-xs ${statusStyles[order.status] || "bg-gray-100 text-gray-600"}`}>{order.status}</span></td><td className="py-3 text-gray-500">{order.date ? new Intl.DateTimeFormat("en-GB", { day: "2-digit", month: "2-digit", year: "2-digit" }).format(new Date(order.date)) : "—"}</td></tr>)}</tbody></table></div>; }
