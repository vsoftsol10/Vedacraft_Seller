import ProductsList from "../components/products/Productlist";

// Page-level route, matching the Dashboard page structure.
// The table, action menu, and product details remain reusable UI components.
export default function Products() {
  return <ProductsList />;
}
